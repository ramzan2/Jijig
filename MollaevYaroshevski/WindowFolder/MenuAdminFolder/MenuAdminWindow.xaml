<Window x:Class="MollaevYaroshevski.WindowFolder.MenuAdminFolder.MenuAdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MollaevYaroshevski.WindowFolder.MenuAdminFolder"
        mc:Ignorable="d"
        Title="Окно администратора" Height="550" Width="1000"
        MaxHeight="550" MaxWidth="1000"
        MinHeight="550" MinWidth="1000"
        Background="#d1eeea"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="150"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Image Source="/MollaevYaroshevski;component/ResourceFolder/ImageFolder/Logo.png"/>

                <StackPanel Grid.Row="1">
                    <Button x:Name="ListBookBtn" Width="260"
                            
                                Click="ListBookBtn_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Список пользователей" 
                                       Style="{DynamicResource ButtonTextBlock}"
                                        Foreground="#2a5674"/>

                        </StackPanel>
                    </Button>
                    <Button x:Name="AddBookBtn" Width="260"
                                Click="AddBookBtn_Click"
                            >
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Добавить пользователя" 
                                       Style="{DynamicResource ButtonTextBlock}"
                                        Foreground="#2a5674"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
                <Button x:Name="ExitBtn" Width="260"
                            HorizontalContentAlignment="Center"
                            VerticalAlignment="Bottom"
                            Grid.Row="1" Click="ExitBtn_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Выйти"
                                   Style="{DynamicResource ButtonTextBlock}"
                                    Foreground="#2a5674"/>
                    </StackPanel>
                </Button>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="70"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Label Style="{DynamicResource LabelNormalStyle}"                          
                           Content="{Binding ElementName=MainFrame,
                        Path=Content.Title}" Height="38"/>
                <Frame Grid.Row="1" Name="MaiFrame" Margin="10"
                           NavigationUIVisibility="Hidden"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
